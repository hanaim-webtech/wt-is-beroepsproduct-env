services:
  database_server:
    platform: "linux/amd64"
    image: mcr.microsoft.com/mssql/server:2022-latest
    env_file:
      - variables.env
    ports:
      - target: 1433
        published: 1434
        protocol: tcp
        mode: host
    volumes:
      - data-volume:/var/opt/mssql
  web_server:
    platform: "linux/amd64"
    image: namoshek/php-mssql:8.4-cli-alpine
    env_file:
      - variables.env
    depends_on:
      - database_server
    ports:
      - target: 80
        published: 8080
        protocol: tcp
        mode: host
    volumes:
      - ./applicatie/:/applicatie/
      - ./webserver-setup/:/setup/
    entrypoint: [ "sh", "/setup/bootstrap.sh" ]
volumes:
  data-volume:
